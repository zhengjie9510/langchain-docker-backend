[project]
name = "langchain-docker-backend"
version = "0.1.0"
description = "Docker sandbox backend for AI agents - execute code safely in isolated containers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.6.12",
    "docker>=7.1.0",
]
